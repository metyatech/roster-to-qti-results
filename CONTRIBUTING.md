# Contributing

Thanks for your interest in improving roster-to-qti-results.

## Development

- Install dependencies: `npm install`
- Run checks: `npm run lint` and `npm test`

## Pull requests

- Keep changes focused and well-scoped.
- Update documentation and tests when behavior changes.
- Ensure `AGENTS.md` is up to date (run `compose-agentsmd`).